# Prospector Scanner（非觸控、dongle 純顯示器）。
# 基底 = t-ogura/zmk-config-prospector 的 config/prospector_scanner.conf，
# 本 repo 的自訂只有最上面這一段；還原方式見 DONGLE-RESTORE.md。

# ============== 本 repo 的自訂 ==============
# layout 3 = Radii：輪盤 + 目前層名（大字）。這是唯一沒有 WPM 手速統計的
# layout（0=YADS 層號列、1=Field 是 WPM 動畫、2=Operator 有 WPM bar）。
# 非觸控模式下 layout 開機固定，只能在這裡改。
CONFIG_PROSPECTOR_DEFAULT_LAYOUT=3

# 輪盤格數上限是 10（Kconfig range 4-10）。鍵盤有 17 層，10 以上的層
# 輪盤不會亮格，但層名照常顯示——SYM 小抄 overlay 也照常觸發。
CONFIG_PROSPECTOR_MAX_LAYERS=10

# SYM 按鍵小抄 overlay 不在這裡設定——它寫死在
# scanner-module/boards/shields/prospector_scanner/src/radii_layout.c
# （CHEAT_LAYER_NAME / cheat_rows），改 SYM 層 bindings 時要去那裡同步。

# ============== 以下照抄 upstream ==============

# ============== Display Configuration ==============
CONFIG_ZMK_DISPLAY=y
CONFIG_DISPLAY=y
CONFIG_LVGL=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# LVGL memory pool
CONFIG_LV_Z_MEM_POOL_SIZE=49152

# Reduce screen flickering with double buffering
CONFIG_LV_Z_DOUBLE_VDB=y
CONFIG_LV_Z_VDB_SIZE=30

# Display thread stack
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
CONFIG_ZMK_DISPLAY_DEDICATED_THREAD_STACK_SIZE=16384
CONFIG_MAIN_STACK_SIZE=4096

# FPU context sharing between threads (prevents FPU register corruption on context switch)
CONFIG_FPU_SHARING=y

# GPIO for backlight
CONFIG_GPIO=y

# ============== LVGL Widgets ==============
CONFIG_LV_USE_LABEL=y
CONFIG_LV_USE_BAR=y
CONFIG_LV_USE_FLEX=y
CONFIG_LV_USE_ARC=y
CONFIG_LV_USE_CANVAS=y
CONFIG_LV_USE_IMAGE=y

# Widgets for settings screens (compiled but not used in non-touch mode)
CONFIG_LV_USE_SLIDER=y
CONFIG_LV_USE_SWITCH=y
CONFIG_LV_USE_BUTTON=y

# ============== Fonts ==============
CONFIG_LV_FONT_MONTSERRAT_12=y
CONFIG_LV_FONT_MONTSERRAT_16=y
CONFIG_LV_FONT_MONTSERRAT_18=y
CONFIG_LV_FONT_MONTSERRAT_20=y
CONFIG_LV_FONT_MONTSERRAT_28=y
CONFIG_LV_FONT_DEFAULT_MONTSERRAT_20=y
CONFIG_LV_FONT_UNSCII_8=y
CONFIG_LV_FONT_UNSCII_16=y

# ============== Scanner Mode ==============
CONFIG_BT=y
CONFIG_BT_OBSERVER=y
CONFIG_PROSPECTOR_MODE_SCANNER=y

# Touch disabled (display-only mode)
CONFIG_PROSPECTOR_TOUCH_ENABLED=n

# Layer slide animation (default: disabled, works in both touch and non-touch modes)
# To enable slide animation by default: CONFIG_PROSPECTOR_LAYER_SLIDE_DEFAULT=y

# ============== PWM Backlight ==============
CONFIG_PWM=y
CONFIG_LED=y
CONFIG_LED_PWM=y

# Stack overflow detection (writes sentinel at stack bottom, checks on context switch)
CONFIG_STACK_SENTINEL=y

# ============== Disable Optional Features ==============
CONFIG_ZMK_SPLIT=n
CONFIG_ZMK_STUDIO=n
CONFIG_PM_DEVICE=n

# Battery and light sensor disabled by default
# To enable: CONFIG_PROSPECTOR_BATTERY_SUPPORT=y
# To enable: CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=y
CONFIG_I2C=y
